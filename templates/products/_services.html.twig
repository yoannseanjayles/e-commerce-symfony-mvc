<!-- Services Area Start -->
<div class="categories-area section-padding40 gray-bg">
    <div class="container-fluid">
        <div class="row">
            {% for i in 1..4 %}
            <div class="col-lg-3 col-md-6 col-sm-6">
                <div class="single-cat {{ i == 3 ? 'mb-30' }}">
                    <div class="cat-icon"><img src="{{ asset('assets/img/icon/services' ~ i ~ '.svg') }}" alt=""></div>
                    <div class="cat-cap"><h5>Fast & Free Delivery</h5><p>Free delivery on all orders</p></div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
<!-- Services Area End -->
